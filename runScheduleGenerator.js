// // runScheduleGenerator.js
// const generateSchedules = require('./server/scheduleGenerator');

// generateSchedules().then(() => {
//   console.log('Schedules generated successfully.');
//   process.exit(0); // Exit the process after generating schedules
// }).catch((error) => {
//   console.error('Error generating schedules:', error);
//   process.exit(1); // Exit the process with an error code
// });
